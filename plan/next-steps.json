{
  "todos": {
    "follow_ups": [
      {
        "date": "2026-06-12",
        "plan": "multi-instance-sandbox",
        "note": "Integration tests for create/list/stop/delete against a live Docker daemon were not written. Follow-up in a future QA plan."
      },
      {
        "date": "2026-06-12",
        "plan": "multi-instance-sandbox",
        "note": "ai-sandbox list when Docker daemon is down prints 'No sandboxes found.' A future improvement could distinguish daemon-not-running from no managed containers."
      },
      {
        "date": "2026-06-15",
        "plan": "plugin-marketplace / clean-slate-mode",
        "note": "profile-installer JSON output tests were not written (partially backfilled in clean-slate-mode Phase 03: flag parsing and generate_volume_override file:// tests were added). Remaining gap: profile-installer node script unit tests."
      },
      {
        "date": "2026-06-13",
        "plan": "plugin-marketplace",
        "note": "mid-task-commit.sh operates on the main checkout rather than the task worktree. Mid-task checkpoints were committed manually instead. Investigate and fix or document."
      },
      {
        "date": "2026-06-15",
        "plan": "clean-slate-mode",
        "task": "phase-02-compose-and-volumes/001-compose-restructuring.md",
        "note": "Validation check 4 (docker compose config parse for docker-compose.mirror-claude.yaml) was not-applicable — no live Docker daemon in task agent context. Low-risk: file mirrors docker-compose.shared-config.yaml. Verify manually or via integration test."
      }
    ],
    "decisions": [
      {
        "date": "2026-06-12",
        "plan": "multi-instance-sandbox",
        "note": "Label-only instance state: no XDG state files. Existence is determined from docker ps -a --filter label=ai.sandbox.managed=true."
      },
      {
        "date": "2026-06-12",
        "plan": "multi-instance-sandbox",
        "note": "stop preserves the container (docker compose stop); delete removes it (docker compose down). cleanup_stale_container treats exited as healthy."
      },
      {
        "date": "2026-06-13",
        "plan": "plugin-marketplace",
        "note": "Plugin marketplace refs are passed through the container environment as pipe-separated strings (AI_SANDBOX_MARKETPLACES, AI_SANDBOX_PLUGINS) rather than colon-separated, because marketplace URLs contain colons."
      },
      {
        "date": "2026-06-13",
        "plan": "plugin-marketplace",
        "note": "The 10-plugin-setup cont-init script runs claude CLI commands as the host user so ~/.claude resolves to the bind-mounted directory with correct ownership."
      },
      {
        "date": "2026-06-15",
        "plan": "clean-slate-mode",
        "note": "--clean always forces EFFECTIVE_MODE=static, overriding MODE_OVERRIDE and profile mode. This ensures ~/.config overlays are never applied in clean mode."
      },
      {
        "date": "2026-06-15",
        "plan": "clean-slate-mode",
        "note": "~/.claude mount extracted to docker-compose.mirror-claude.yaml overlay, included by default and skipped in clean mode. Mirrors the ~/.config overlay pattern."
      },
      {
        "date": "2026-06-15",
        "plan": "clean-slate-mode",
        "note": "Clean mode suppresses implicit host state (plugin dir mounts, ~/.claude bind-mount) but NOT explicitly-requested resources (file:// marketplace mounts still applied via generate_volume_override)."
      }
    ],
    "blockers": []
  }
}
