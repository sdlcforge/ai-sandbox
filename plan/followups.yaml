version: 1
items:
    - title: 'Pre-existing latent bug, not touched (out of '
      id: bgCr
      date: "2026-07-09"
      tag: profiles-resource
      text: '[phase-02-profiles-resource/002-complete-name-resolution-and-verb-gating.md] Pre-existing latent bug, not touched (out of scope, unrelated to this task''s Requirements): src/profiles.sh''s profiles_create() -> _cp_entries_to_json() uses `local -n` (bash 4.3+ nameref syntax), which throws under macOS system bash 3.2 (bin/ai-sandbox.sh''s shebang target) — harmlessly swallowed in the smoke-tested empty-array case, but would silently produce empty skills/hooks/agents JSON for a profile that has real discoverable entries. Introduced in phase-02 task 001, which fixed two other bash-3.2 portability issues in this same file but missed this one.'
    - title: docs/architecture.md step 1 still lists NO_CH
      id: PVKp
      date: "2026-07-09"
      tag: docs-and-help
      text: '[phase-03-docs-and-help/002-update-architecture-and-help-text.md] docs/architecture.md step 1 still lists NO_CHROMIUM/NO_DOCKER/ENABLE_DOCKER_PROXY as globals populated by options.sh; these flags were removed in the restructure (now error with a redirect message). Predates and is unrelated to this task''s scoped surface (noun-dispatch/status-detail/attach-connect) — not caught by the task''s grep list. Worth a separate follow-up task.'
    - title: 'src/help.sh''s per-instance commands list and '
      id: cfdc
      date: "2026-07-09"
      tag: docs-and-help
      text: '[phase-03-docs-and-help/002-update-architecture-and-help-text.md] src/help.sh''s per-instance commands list and architecture.md''s step-11 dispatch-word enumeration both omit the ''up'' word (present in PER_INSTANCE_COMMANDS) — this gap predates the CLI restructure entirely (never documented before either) and was out of this task''s named scope.'
    - title: --quiet leaks docker-check output to stdout
      id: Trit
      date: "2026-07-10"
      tag: fix-dockerfile-path-drift
      text: Discovered while fixing followup J9mV (Dockerfile PATH drift), confirmed pre-existing on baseline via git stash A/B test — unrelated to that fix, out of scope for it. Under --quiet, several user-exec/root-exec invocations still print "Checking docker is running... confirmed." to stdout (src/utils.sh:42 area — qecho -n "Checking docker is running... " followed by an apparently un-guarded echo/print of "confirmed."). This pollutes stdout for tests using exact-match assertions (`The output should eq ...`), causing 10 failures in test/integration/container_spec.sh (sandbox-volumes status, Bun, User UID, Firewall rules, SSH agent forwarding, DEVCONTAINER env, Config isolation) plus similar failures observed in lifecycle_spec.sh, clean_container_spec.sh, and docker_proxy_spec.sh during a full `make test.integration` run. Needs investigation of the docker-running check path to properly gate the "confirmed." output behind QUIET.
    - title: 'Integration suite: BeforeAll/status failures'
      id: zSwd
      date: "2026-07-10"
      tag: fix-dockerfile-path-drift
      text: 'Discovered while fixing followup J9mV, out of scope for that task — not investigated further. A full `make test.integration` run shows BeforeAll hook errors in clean_container_spec.sh and docker_proxy_spec.sh, and several lifecycle_spec.sh failures referencing "Error: ''status'' is not a known instance or profile". These look like separate, pre-existing test/CLI issues — possibly related to or compounding the same --quiet stdout-pollution bug (see sibling followup "--quiet leaks docker-check output to stdout"), or a separate `status` subcommand naming/resolution issue. Needs triage to determine root cause(s) and whether it''s one bug or several.'
