version: 1
items:
    - title: 'Pre-existing latent bug, not touched (out of '
      id: bgCr
      date: "2026-07-09"
      tag: profiles-resource
      text: '[phase-02-profiles-resource/002-complete-name-resolution-and-verb-gating.md] Pre-existing latent bug, not touched (out of scope, unrelated to this task''s Requirements): src/profiles.sh''s profiles_create() -> _cp_entries_to_json() uses `local -n` (bash 4.3+ nameref syntax), which throws under macOS system bash 3.2 (bin/ai-sandbox.sh''s shebang target) — harmlessly swallowed in the smoke-tested empty-array case, but would silently produce empty skills/hooks/agents JSON for a profile that has real discoverable entries. Introduced in phase-02 task 001, which fixed two other bash-3.2 portability issues in this same file but missed this one.'
    - title: docs/architecture.md step 1 still lists NO_CH
      id: PVKp
      date: "2026-07-09"
      tag: docs-and-help
      text: '[phase-03-docs-and-help/002-update-architecture-and-help-text.md] docs/architecture.md step 1 still lists NO_CHROMIUM/NO_DOCKER/ENABLE_DOCKER_PROXY as globals populated by options.sh; these flags were removed in the restructure (now error with a redirect message). Predates and is unrelated to this task''s scoped surface (noun-dispatch/status-detail/attach-connect) — not caught by the task''s grep list. Worth a separate follow-up task.'
    - title: 'src/help.sh''s per-instance commands list and '
      id: cfdc
      date: "2026-07-09"
      tag: docs-and-help
      text: '[phase-03-docs-and-help/002-update-architecture-and-help-text.md] src/help.sh''s per-instance commands list and architecture.md''s step-11 dispatch-word enumeration both omit the ''up'' word (present in PER_INSTANCE_COMMANDS) — this gap predates the CLI restructure entirely (never documented before either) and was out of this task''s named scope.'
    - title: Dockerfile vs Dockerfile.base PATH drift
      id: J9mV
      date: "2026-07-10"
      tag: fix-enable-plugin-bug
      text: 'Discovered while adding test/integration/plugin_install_spec.sh: docker/Dockerfile (apparently unused for real builds) has an image-level `ENV PATH="${HOST_HOME}/.bun/bin:...:${PATH}"` line, but the actual Dockerfile used by builds — docker/Dockerfile.base, assembled via docker/scripts/assemble-dockerfile.sh — only appends those PATH entries to ~/.zshenv, not an image-level ENV. Consequence: `claude` (and other .bun/bin/.local/bin/go/bin tools) fail to resolve via a bare `sh -c`/argv `user-exec <cmd>` invocation inside the container; only `zsh -c` or an interactive/login shell (which sources ~/.zshenv) picks it up. This independently breaks the pre-existing test/integration/container_spec.sh:124 assertion ("Claude Code is installed and on PATH", via `user-exec claude --version` with no shell) — confirmed via direct testing, unrelated to any change in this task. Recommend either deleting the unused docker/Dockerfile or reconciling its PATH handling with Dockerfile.base, and fixing the container_spec.sh assertion to match actual container PATH behavior (or switching it to a login/zsh invocation).'
    - title: enter_spec uses stale bare create form
      id: gGDr
      date: "2026-07-10"
      tag: fix-enable-plugin-bug
      text: 'test/integration/named_instance_enter_spec.sh''s create_instance() still uses the pre-CLI-restructure bare `create enter-test ...` invocation. Confirmed broken against the current noun-based grammar (src/options.sh no longer treats "create" as a free-standing global command word — it''s now reserved and only reachable via "instances create <name>"): running it now errors with "Error: ''create'' is a reserved name and cannot be used as a sandbox name". Discovered incidentally while adding test/integration/plugin_install_spec.sh (which correctly uses "instances create"); out of scope for that task. Needs updating to `./bin/ai-sandbox.sh instances create enter-test --mode static --clean --quiet`.'
