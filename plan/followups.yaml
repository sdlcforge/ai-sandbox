version: 1
items:
    - title: Flags-before-command-word mis-parse
      id: sSU2
      date: "2026-07-06"
      tag: fix-name-flags-parsing
      text: 'parse_options() mis-routes when a config flag precedes the per-instance command word, e.g. `ai-sandbox myname --profile x start`. Reproduced via scratch probe: result is SANDBOX_NAME=myname, CMD=enter (not start), PROFILES=(x), ARGS=(start) — "start" is silently absorbed into the passthrough ARGS array instead of being recognized as the per-instance command. Structurally similar to the just-fixed bug (name-followed-directly-by-flags defaulting CMD to enter), but here Phase 3''s flag parser has no mechanism to promote a later bare word back into CMD. Needs its own scoped fix.'
    - title: 'Task agent hit a mid-response API disconnect '
      id: S6Up
      date: "2026-07-06"
      tag: restore-fix
      text: '[phase-01-restore-fix/001-restore-mode-and-clean-slate-on-bare-enter.md] Task agent hit a mid-response API disconnect (Connection closed mid-response) after committing its work but before returning its structured report. The manager verified the commit directly (git show, make lint, make test.unit) and confirms it fully satisfies the task doc''s Requirements and Validation sections. No re-dispatch was necessary.'
    - title: Combine sequential docker inspect calls
      id: 4DzF
      date: "2026-07-06"
      tag: restore-fix
      text: 'Phase-review (efficiency lens, non-blocking suggestion): restore_saved_config() in src/utils.sh issues three sequential `docker inspect -f ...` calls (one per label: profiles, mode, clean-slate) where a single multi-field format string would do, saving two subprocess spawns + Docker API round-trips per bare start/enter invocation. Noted as consistent with the existing convention in running_config_matches() (six sequential calls, unchanged), so this is a pre-existing pattern being extended, not a new regression — purely a cold/warm-path nicety, not correctness-affecting. If addressed, do both functions together in the same pass for consistency (expands scope beyond a single trivial edit, hence deferred rather than dispatched immediately).'
    - title: plan_register/deregister no-op vs .flow/
      id: HSjz
      date: "2026-07-06"
      tag: flow-tooling
      text: .flow/ is gitignored in this repo (see commit 55bc14b, "update to ignore 'worktrees' and '.flow' dirs"). mcp__flow-mcp__plan_register and plan_deregister both claim to commit .flow/plans/<slug>.json changes to the working branch via `git rm`/`git add`, but since the path is untracked+ignored, that step silently no-ops — plan_register never actually creates a git-tracked record, and plan_deregister returned a success record for `enter-mode-restore` without removing the file from disk (confirmed via direct `ls`/`test -f` after the call). Had to manually `rm .flow/plans/enter-mode-restore.json` during this session's plan finalization. This is a Flow-tooling gap, not specific to this bugfix plan — worth fixing in the flow-mcp server itself (e.g. detect the gitignored case and fall back to a plain filesystem write/delete) or reconsidering whether .flow/ should be gitignored at all, since other historical plans (clean-slate-mode, plugin-marketplace, multi-instance-sandbox) DO have committed open/close-plan-worktree commits from before the .flow/ gitignore rule was added.
    - title: Duplicate jq array conversion in index.sh
      id: 85Na
      date: "2026-07-06"
      tag: full-config-restore
      text: 'Phase-review (efficiency lens, minor/high-confidence, non-blocking): src/index.sh''s new config-persistence block (~lines 146-160) recomputes the same jq -R . | jq -s . array-JSON conversion for CLI_MARKETPLACES/CLI_PLUGINS that the pre-existing CLI-merge block (~lines 112-132) already computed into _cli_marketplaces_json/_cli_plugins_json. Hoist/reuse instead of recomputing. Invocation-scoped (not looped), so low materiality — bundle with other src/index.sh cleanup rather than fixing standalone.'
    - title: Consolidate per-label docker inspect calls
      id: o0pc
      date: "2026-07-06"
      tag: full-config-restore
      text: 'Phase-review (efficiency lens, minor/medium-confidence, non-blocking, extends existing followup 4DzF): running_config_matches() now issues 9 sequential single-field `docker inspect` calls (was 6, +3 from the marketplaces/plugins/enable-all reconciliation task) where a single multi-field --format template would do. Explicitly directed by the phase''s design note as a deliberate style-consistency choice, not a regression — bundle with 4DzF''s existing docker-inspect-consolidation followup for both restore_saved_config and running_config_matches together.'
    - title: No size bound on config label decode
      id: qVbA
      date: "2026-07-06"
      tag: full-config-restore
      text: 'Phase-review (security lens, suggestion/low-confidence, non-blocking): restore_saved_config and status.sh''s config decode impose no size bound on the decoded ai.sandbox.config label before base64-decoding/jq-parsing it. Label is only writable at container-create time by the host process, so practical risk is low; optional defense-in-depth would bound the decoded payload size before piping into jq.'
    - title: Task document Status section could not be upd
      id: AGul
      date: "2026-07-06"
      tag: full-config-restore
      text: '[phase-01-full-config-restore/001-persist-and-restore-full-config-inputs.md] Task document Status section could not be updated by the task agent (worktree boundary — task doc lives only in the plan worktree). Digest captured here via outcome_digest instead; no action needed.'
    - title: Confirmed running_config_matches extension (m
      id: L9e6
      date: "2026-07-06"
      tag: full-config-restore
      text: '[phase-01-full-config-restore/001-persist-and-restore-full-config-inputs.md] Confirmed running_config_matches extension (marketplaces/plugins/enable-all) is intentionally out of this task''s scope, covered by task 002.'
    - title: Task document Status section could not be upd
      id: LruS
      date: "2026-07-06"
      tag: full-config-restore
      text: '[phase-01-full-config-restore/002-reconcile-running-config-match.md] Task document Status section could not be updated by the task agent (same worktree-boundary limitation as task 001). Status: succeeded, 2026-07-06, 136 examples 0 failures, files: docker/docker-compose.yaml, src/utils.sh, test/unit/ai_sandbox_spec.sh. No action needed beyond noting for the record.'
    - title: Task document Status section could not be upd
      id: 792W
      date: "2026-07-06"
      tag: full-config-restore
      text: '[phase-01-full-config-restore/003-add-detail-alias-and-config-display.md] Task document Status section could not be updated (same worktree-boundary limitation as tasks 001/002). Status: succeeded, 2026-07-06, 145 examples 0 failures, files: src/options.sh, src/status.sh, README.md, test/unit/ai_sandbox_spec.sh.'
    - title: 'Pre-existing, out-of-scope observation: RESER'
      id: HTL6
      date: "2026-07-06"
      tag: full-config-restore
      text: '[phase-01-full-config-restore/003-add-detail-alias-and-config-display.md] Pre-existing, out-of-scope observation: RESERVED_NAMES in src/options.sh is unreachable for any word also in PER_INSTANCE_COMMANDS (e.g. `ai-sandbox create detail` succeeds with SANDBOX_NAME=detail instead of being rejected — same pre-existing gap already affects `create status`). Not introduced by this task; fixing would touch the create-branch logic affecting multiple existing commands. Recommend a followup rather than expanding this task''s scope.'
    - title: Task document Status section could not be upd
      id: Lnj3
      date: "2026-07-06"
      tag: doc-updates
      text: '[phase-02-doc-updates/001-update-architecture-docs.md] Task document Status section could not be updated (same worktree-boundary limitation as prior tasks in this plan). No action needed.'
    - title: 'Manager note (not from task agent): the task '
      id: 2e9z
      date: "2026-07-06"
      tag: doc-updates
      text: '[phase-02-doc-updates/001-update-architecture-docs.md] Manager note (not from task agent): the task agent''s changes were initially left uncommitted in the worktree; manager reviewed the diff against the reported work and committed it directly (d6b8412) before applying this report.'
